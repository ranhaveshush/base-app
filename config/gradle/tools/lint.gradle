android {
    lintOptions {
        abortOnError true

        ignoreWarnings false

        lintConfig file("$project.rootDir/config/lint/lint-rules.xml")

        xmlReport false
        htmlReport true
        htmlOutput file("build/reports/lint/lint-results.html")
    }
}